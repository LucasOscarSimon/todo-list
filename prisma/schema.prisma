generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model tarea {
  id          Int    @id @unique @default(autoincrement())
  nombre      String
  descripcion String
}
